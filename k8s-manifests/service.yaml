apiVersion: v1
kind: Service
metadata:
  name: python-backend-service
  namespace: default
  labels:
    app: python-backend
spec:
  selector:
    app: python-backend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: ClusterIP
